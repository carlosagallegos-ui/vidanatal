{
  "name": "ActividadCompletada",
  "type": "object",
  "properties": {
    "perfil_id": {
      "type": "string"
    },
    "identificador": {
      "type": "string"
    },
    "titulo": {
      "type": "string"
    },
    "tipo": {
      "type": "string"
    },
    "fecha_completada": {
      "type": "string",
      "format": "date"
    },
    "unidad_medica": {
      "type": "string"
    },
    "notas": {
      "type": "string"
    }
  },
  "required": [
    "perfil_id",
    "identificador"
  ],
  "rls": {
    "create": {
      "created_by_id": "{{user.id}}"
    },
    "read": {
      "created_by_id": "{{user.id}}"
    },
    "update": {
      "created_by_id": "{{user.id}}"
    },
    "delete": {
      "created_by_id": "{{user.id}}"
    }
  }
}